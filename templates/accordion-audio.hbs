<div class="component__inner accordionaudio__inner">

  {{> component this}}

  <div class="component__widget accordionaudio__widget">

    {{#each _items}}
    <div class="accordionaudio__item{{#if _graphic.src}} has-image{{/if}}{{#if _classes}} {{_classes}}{{/if}}" data-index="{{_index}}">

      <button id="{{../_id}}-{{@index}}-accordionaudio-button" class="accordionaudio__item-btn js-toggle-item is-closed{{#if _isVisited}} is-visited{{/if}}" aria-expanded="false">
        <div class="accordionaudio__item-btn-inner">

          <div class="accordionaudio__item-icon">
            <div class="icon"></div>
          </div>

          <div class="accordionaudio__item-title">
            <div class="accordionaudio__item-title-inner">
              {{{title}}}
            </div>
          </div>

        </div>
      </button>

      <div class="accordionaudio__item-content" role="region" aria-labelledby="{{../_id}}-{{@index}}-accordionaudio-button">
        <div class="accordionaudio__item-content-inner">

          {{#if body}}
          <div class="accordionaudio__item-body">
            <div class="accordionaudio__item-body-inner">
              {{{compile body}}}
            </div>
          </div>
          {{/if}}

          {{#if _graphic.src}}
          <div class="accordionaudio__item-image-container{{#if _graphic.attribution}} has-attribution{{/if}}">
            <img class="accordionaudio__item-image" src="{{_graphic.src}}" {{#if _graphic.alt}}aria-label="{{_graphic.alt}}"{{else}}aria-hidden="true"{{/if}}>

            {{#if _graphic.attribution}}
            <div class="component__attribution accordionaudio__attribution">
              <div class="component__attribution-inner accordionaudio__attribution-inner">
                {{{_graphic.attribution}}}
              </div>
            </div>
            {{/if}}

          </div>
          {{/if}}

        </div>
      </div>

    </div>
    {{/each}}

  </div>
</div>
